import 'package:flutter/material.dart';

class goiY extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Trang gợi ý'),
      ),
      body: Center(
        child: Text('Đây là trang gợi ý'),
      ),
    );
  }
}
