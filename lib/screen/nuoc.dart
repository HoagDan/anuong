import 'package:flutter/material.dart';

class Nuoc extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Trang nước'),
      ),
      body: Center(
        child: Text('Đây là trang nước'),
      ),
    );
  }
}
